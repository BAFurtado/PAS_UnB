This is a simple repository to READ PDF file of results from PAS (exam) at UnB

Extracts the data and dumps into a DATAFRAME

The data is available as redacao.pdf

# Given that PDF pages have page numbers and disturbs the order of the data, some of unused was corrected manually.
# Up to a REGEX expert to do it all automatically